## 3 — Intro to Git: Install Git & Create a Local Repository

**Goal:** Learn how to install Git, set up your identity, and create your first local repository for the Helpdesk project.
Git will allow you to **track changes**, **collaborate**, and later **connect with GitHub** for CI/CD.

---

### 🔹 Step 1: Check if Git is Already Installed

Before installing, check whether Git is already available.

**Run this command:**

```bash
git --version
```

✅ **Expected output example:**

```
git version 2.45.0
```

If you get an error or message like *‘command not found’*, Git is not installed.
Proceed to install it.

---

### 🔹 Step 2: Install Git (If Not Installed)

**For Windows:**

1. Go to [https://git-scm.com/downloads](https://git-scm.com/downloads)
2. Download the **Windows Installer**.
3. Run the setup and choose:

   * ✅ “Use Git from Windows Command Prompt”
   * ✅ “Checkout Windows-style, commit Unix-style line endings”
   * ✅ “Use the default editor (Vim or Nano)” (you can change later)

**For macOS:**

```bash
brew install git
```

(or install Xcode Command Line Tools if you prefer)

**For Linux:**

```bash
sudo apt update
sudo apt install git
```

After installation, verify again:

```bash
git --version
```

✅ Git is now installed and ready to use.

---

### 🔹 Step 3: Configure Git with Your Name and Email

Git identifies you by your **name** and **email** in every commit.
These will later match your GitHub identity.

**Run these commands (replace with your info):**

```bash
git config --global user.name "Your Name"
git config --global user.email "youremail@example.com"
```

Verify:

```bash
git config --list
```

✅ You should see:

```
user.name=Your Name
user.email=youremail@example.com
```

💡 *Tip:* You only need to do this once per system.
Git will use these details for all your projects.

---

### 🔹 Step 4: Initialize a Local Git Repository

Now let’s create a local repository for your Django project.

1. Make sure you are inside your project folder:

   ```bash
   cd helpdesk_project
   ```

2. Initialize Git:

   ```bash
   git init
   ```

✅ This creates a hidden `.git` folder that tracks every change you make.

3. Check the repository status:

   ```bash
   git status
   ```

Expected output:

```
On branch master

No commits yet
nothing to commit (create/copy files and use "git add" to track)
```

Your local Git repo is ready!

---

### 🔹 Step 5: Create Your First Commit

Let’s make your **first commit** — a snapshot of your starting point.

1. Create a `.gitignore` file to exclude unwanted files:

   ```bash
   echo "venv/" > .gitignore
   echo "__pycache__/" >> .gitignore
   echo "*.sqlite3" >> .gitignore
   echo "*.pyc" >> .gitignore
   echo "staticfiles/" >> .gitignore
   ```

2. Stage files:

   ```bash
   git add .
   ```

3. Commit:

   ```bash
   git commit -m "Initial commit: setup virtual environment and project folder"
   ```

✅ Output should confirm:

```
[master (root-commit) 123abc4] Initial commit: setup virtual environment and project folder
```

4. Check commit history:

   ```bash
   git log --oneline
   ```

You’ll see your first commit listed.

---

### 🧭 Summary

| Step | Action                  | Result                      |
| ---- | ----------------------- | --------------------------- |
| 1    | Checked Git version     | Verified Git presence       |
| 2    | Installed Git           | Installed system-wide       |
| 3    | Configured name & email | Set up identity             |
| 4    | Initialized local repo  | Started Git tracking        |
| 5    | Created first commit    | Saved initial project state |

---

💡 **Key Concept:**

> Git is your **time machine** for code. Every commit is a save point you can return to if something breaks.
